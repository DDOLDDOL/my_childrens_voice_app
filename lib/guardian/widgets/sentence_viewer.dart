import 'package:flutter/material.dart';

class SentenceViewer extends StatelessWidget {
  const SentenceViewer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
